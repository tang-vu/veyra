{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window access only; Veyra authority stays behind the authenticated loopback API.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
